# PaperDoctor — minimal environment
# Core pipeline: prepare-paper → verify-* → write-summary → write-review

# PDF → Markdown conversion (tools/pdf_processor.py)
magic-pdf[full]>=1.3.0

# PDF page rendering (tools/pdf_render.py)
pdf2image>=1.17.0

# Code indexing via AST (tools/code_analyzer.py)
tree-sitter>=0.23.0
tree-sitter-python>=0.23.0
tree-sitter-javascript>=0.23.0
tree-sitter-java>=0.23.0
tree-sitter-go>=0.23.0
tree-sitter-c>=0.23.0
tree-sitter-cpp>=0.23.0
tree-sitter-rust>=0.23.0

# HTTP requests for reference verification (skills/read-ref)
requests>=2.31.0

# PDF annotation + layout check (tools/pdf_annotator.py, write-revise, pdf-layout)
PyMuPDF>=1.24.0

# --- Optional (uncomment as needed) ---

# MCP server mode (mcp_server.py)
# fastmcp>=0.1.0

# Format checking - verify-format skill
# numpy>=1.26.0
# pypdf>=4.0.0

# Review web UI - review-ui skill
# streamlit>=1.30.0
# streamlit-pdf-viewer>=0.0.7
